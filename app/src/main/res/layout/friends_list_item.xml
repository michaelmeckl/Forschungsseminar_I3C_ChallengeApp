<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    card_view:cardElevation="4dp"
    card_view:cardUseCompatPadding="true">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/category_item"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="4dp">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/profile_picture"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintTop_toTopOf="@id/category_item"
            app:layout_constraintBottom_toBottomOf="@id/category_item"
            app:layout_constraintLeft_toLeftOf="@id/category_item"
            app:layout_constraintRight_toLeftOf="@id/friend_name"
            android:elevation="2dp"
            android:src="@drawable/test" />

        <TextView
            android:id="@+id/friend_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            app:layout_constraintTop_toTopOf="@id/category_item"
            app:layout_constraintBottom_toBottomOf="@id/category_item"
            app:layout_constraintLeft_toRightOf="@id/profile_picture"
            android:textAppearance="@style/TextAppearance.AppCompat.Large"
            tools:text="Freund" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>